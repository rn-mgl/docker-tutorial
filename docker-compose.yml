services:
  node-app:
    build: .
    env_file:
      - .env
    depends_on:
      - database

  nginx:
    image: nginx:1.29
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro

  database:
    image: mysql:9

  redis:
    image: redis:8
